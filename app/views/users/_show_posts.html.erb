<div class="row">
  <% objects.each do |object| %>
    <div class="col-lg-4 col-md-6 col-sm-12">
      <div class="card mb-3 bike_image_index">
        <%= link_to image_tag(object.image, class: 'img', alt: 'bike image'), post_path(object.id) %>
      </div>
    </div>
  <% end %>
</div>