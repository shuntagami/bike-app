<div class="row">
  <% objects.each do |object| %>
    <div class="col-lg-4 col-md-6 card-index">
      <div class="card">
        <%= link_to image_tag(object.image, class: 'bike_image_index', alt: 'avater image'), post_path(object.id) %>
      </div>
    </div>
  <% end %>
</div>