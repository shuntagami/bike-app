<nav class="navbar navbar-expand-lg navbar-light sticky-top" style="background-color: #FFFFFF;">
    <%= image_tag 'logo.png', :size => '30x30' %>
    <a class="navbar-brand " href="/">Navbar</a>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav ">
        <% if user_signed_in? && current_user.admin?  %>
        <li class="nav-item">
          <%= link_to 'すべてのユーザー', users_path, class: 'nav-link' %>
        </li>
        <% end %>
        <% if user_signed_in? %>
          <%= link_to "新規投稿", new_post_path, class: "nav-link" %>
          <li class="nav-item dropdown">
            <%= link_to " #{current_user.name}さん", '#', class: "nav-link dropdown-toggle", id: "navbarDropdown", role: "button", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" } %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to  'マイページ', user_path(current_user.id), class: 'dropdown-item' %>
              <%= link_to  'ログアウト', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            </div>
          </li>
        <% else %>
          <li class="nav-item d-flex flex-row">
            <%= link_to 'Register', new_user_registration_path, class: "nav-link", "data-toggle": "modal", "data-target": "#signUpModal"%>
            <%= link_to "Login", new_user_session_path, class: "nav-link", "data-toggle": "modal", "data-target": "#logInModal" %>
          </li>
        <% end %>
      </ul>
    </div>
</nav>
<%= render "shared/error_messages" %>
<%= render "shared/success_messages" %>
<%= render "shared/notice_messages" %>

